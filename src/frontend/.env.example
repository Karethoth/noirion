# Backend API URL
# This should point to your GraphQL API endpoint
VITE_API_URL=http://localhost:4000

# Frontend debugging (optional)
# Enables verbose console logging for GraphQL requests
VITE_DEBUG_GRAPHQL=false

# Abort/timeout GraphQL requests after this many ms (helps surface hung requests)
VITE_GRAPHQL_TIMEOUT_MS=30000
